package com.chithanh.italk.notification.enums;

public enum NotificationType {
  SYSTEM,         // Thông báo hệ thống (daily challenge, admin)
  INTERACTION,    // Thông báo tương tác (comment, like, follow)
  REMINDER        // Nhắc nhở (nhắc tham gia challenge)
}